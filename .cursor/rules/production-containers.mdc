---
description: 生产容器操作禁令（天字一号）
globs:
alwaysApply: true
---

# 生产容器操作禁令

## ⚠️ Mac 本地 vs 服务器 ID 不同！

- **Mac 本地 carher-101/102/103** = 测试容器（tester/tester2/tester3）→ 可操作
- **Mac 本地 carher-3** = 董事长 → 🚫 禁止
- carher-4（浏览器测试）已于 2026-03-23 删除
- **服务器 carher-1** = 董事长老杨 → 🚫 禁止

## 🚫 绝对禁止（无任何例外）

**没有用户明确口头指令，禁止对以下容器执行任何操作：**

- **Mac 本地** `carher-3`（董事长）—— 任何 stop / down / restart / rm / kill
- **服务器** `carher-1`（董事长/老杨）—— 任何 stop / down / restart / rm / kill

包括但不限于：
- Mac: `docker stop carher-3` / `./start-user.sh --id=3` / `--id=3 --down`
- 服务器: `docker stop carher-1` / `./start-user.sh --id=1` / `--id=1 --down`
- 任何对上述容器的破坏性命令

**Mac 本地 carher-101/102/103 是测试容器，不受此禁令限制。**

## ✅ 唯一例外

用户在当前对话中明确说出操作上述生产容器的指令，且该指令是针对本次操作的新指令（不是计划文档里的"验证步骤"）。

## 根因记录

2026-02-18：AI 擅自执行 `./start-user.sh --id=3 --down` 停掉了董事长的生产容器，导致服务中断。
2026-02-25：明确 Mac docker1=测试、服务器 docker1=董事长，避免混淆。
2026-03-23：Mac 测试容器从 id=1 迁移到 id=101，id=101→102，新增 id=103，统一 101-103 号段。
