---
name: upgrade-model
description: Upgrade the LLM model used by OpenClaw. Use when the user asks to change, upgrade, or switch the AI model, or mentions a new model version like opus 4.6, gpt-5, gemini-3, etc.
---

# Upgrade OpenClaw LLM Model

## CarHer Docker Source Of Truth (Critical)

When changing model for `carher-N` containers, treat this as a source-of-truth problem, not a runtime file edit:

- Runtime `/data/.openclaw/openclaw.json` is a read-only bind mount.
- Real mounted source is `/tmp/carher-config-N.json`.
- `/tmp/carher-config-N.json` is generated by `start-user.sh` from:
  1. CLI flag `--model=...` (highest priority)
  2. `docker/users.csv` model field (default per user)
  3. `docker/carher-config.json` fallback

Never edit model by writing inside container files. It is not persistent and may fail due to read-only mount.

### Deterministic Model Switch For Any User

1. Inspect current registry row for target `id=N` in `docker/users.csv`.
2. Decide persistence:
   - One-time switch: `./start-user.sh --id=N --model=opus`
   - Persistent default: update `docker/users.csv` model column for `id=N`
3. Recreate container from source:
   - `./start-user.sh --id=N --model=opus --no-rebuild`
   - or `./start-user.sh --id=N --no-rebuild` (if csv already updated)
4. Verify effective model:
   - `docker exec carher-N python3 -c 'import json; d=json.load(open("/data/.openclaw/openclaw.json")); print(d["agents"]["defaults"]["model"]["primary"])'`

### Quick Mapping

- `sonnet` -> `openrouter/anthropic/claude-sonnet-4`
- `opus` -> `openrouter/anthropic/claude-opus-4.6`

## Why It's Not Just a String Change

OpenClaw has a **model registry** (`@mariozechner/pi-ai` → `models.generated.js`) that maps model IDs to metadata (api protocol, contextWindow, cost, etc). If the model isn't in the registry, you get `Unknown model` error.

## Upgrade Path

1. **Check if upstream `@mariozechner/pi-ai` latest version already has the model** — inspect `models.generated.js` in the latest npm tarball.

2. **If yes** → upgrade all four `@mariozechner/pi-*` packages (`pi-agent-core`, `pi-ai`, `pi-coding-agent`, `pi-tui`) to the same version. They must stay in sync.

3. **If no** → add a temporary inline model definition in `openclaw.json` under `models.providers`. Copy metadata from the closest known model, update `id`/`contextWindow` from OpenRouter API. Remove this hack once upstream catches up.

4. **Set the model** in `openclaw.json` → `agents.defaults.model.primary`.

5. **Verify** — gateway log should show the new model name without `Unknown model` errors. Config changes hot-reload; dependency upgrades need `start.sh` restart.
